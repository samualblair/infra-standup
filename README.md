# infra-standup
Information that is 'public' for infrastructure standup

To Use The Configuration files:
1.  Edit the file to match specific requirements.
2.  Save as file named nixos-configuration.nix
3.  Run nixos-rebuild switch to build and switch to the new configuration.  This will take a while the first time as it downloads and builds all the packages.
